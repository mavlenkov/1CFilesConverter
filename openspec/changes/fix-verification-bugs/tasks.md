## 1. Исправление ext2edt.sh

- [x] 1.1 Обернуть блок load_cfe (строки 128–140) в условие `if [[ "${V8_SRC_TYPE}" == "cfe" ]]`, чтобы LoadCfg не вызывался для XML-источника
- [x] 1.2 Убедиться, что подготовка базовой ИБ (parse_base_ib, prepare_base_config) по-прежнему выполняется для обоих типов (cfe и xml)

## 2. Исправление ext2xml.sh

- [x] 2.1 Обернуть блок load_cfe (строки 127–139) в условие `if [[ "${V8_SRC_TYPE}" == "cfe" ]]`, чтобы LoadCfg не вызывался для EDT-источника
- [x] 2.2 Убедиться, что после пропуска LoadCfg для EDT выполняется run_edt_export и корректный выход

## 3. Обновление спецификации

- [x] 3.1 Синхронизировать delta spec в openspec/specs/extension-conversion/spec.md

## 4. Финализация

- [ ] 4.1 Закоммитить исправления
